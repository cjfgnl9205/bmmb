/*console.log("*****nyt-ppg Module........");

var ppgService = (function() {     
	function getEntry(param, callback, error) {
		
		
		$.getJSON("./reply/list/" + ebook_ID + "/" + sno + "/" + eno + ".json",
				function(data) {
					alert(data);
					if (callback) {
						callback(data); // 댓글 목록만 가져오는 경우
						// callback(data.replyCnt, data.list); //댓글 숫자와 목록을 가져오는
						// 경우
					}
				}
		);

	}
	

	return {
		getEntry : getEntry,

	}

})();
*/